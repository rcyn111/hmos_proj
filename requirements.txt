# HarmonyOS Dev Claw — Python 依赖
# 安装方式: pip install -r requirements.txt

# 文章批量生成器依赖
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
anthropic>=0.30.0

# 可选依赖（用于未来扩展）
# requests>=2.28.0       # HTTP 请求（备用）
# beautifulsoup4>=4.12.0 # HTML 解析（备用）
